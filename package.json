{
  "name": "block-mode-for-x",
  "version": "0.1.0",
  "description": "Select accounts on X and process block or mute actions faster.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "@types/chrome": "^0.0.268"
  }
}
